query GetDimension {
  getDimension(dimType: manualPrice)
    @context(provider: "vtex.order-auth-graphql") {
    ruleCollection {
      trigger {
        effect {
          description
          effectType
        }
        condition {
          conditionType
          lessThan
          greatherThan
        }
      }
    }
  }
}

query GetChildrenQuotes(
  $id: String
  $page: Int
  $pageSize: Int
  $sortOrder: String
  $sortedBy: String
) {
  getChildrenQuotes(
    id: $id
    page: $page
    pageSize: $pageSize
    sortOrder: $sortOrder
    sortedBy: $sortedBy
  ) @context(provider: "vtex.b2b-quotes-graphql") {
    data {
      id
      referenceName
      creatorEmail
      creatorRole
      creationDate
      expirationDate
      lastUpdate
      subtotal
      status
      organization
      organizationName
      costCenter
      costCenterName
      viewedBySales
      viewedByCustomer
      seller
      parentQuote
    }
    pagination {
      total
    }
  }
}
